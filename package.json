{
  "devDependencies": {
    "@faker-js/faker": "^10.5.0",
    "cypress": "^15.18.1",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^11.0.0"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:chrome": "cypress run --browser chrome",
    "cy:record": "dotenv -e .env -- cypress run --record"
  }
}
